---
en:
  decidim:
    trusted_ids:
      providers:
        # Omniauth providers are also used as the authorization name for the "trusted_ids_handler", see zz_fallbacks.rb
        valid:
          name: VÀLid
          description: VÀLid is the digital identity service of the Government of Catalonia.
        default:
          name: Trusted IDs
          description: This authorization method is granted to all users that use a Trusted ID login method.
      verifications:
        form:
          cancel: Cancel authorization
    authorization_handlers:
      trusted_ids_handler:
        # "name" and "explanation" are here for reference only, they are resolved dynamically in zz_fallbacks.rb
        explanation: This authorization method is granted to all users that use a Trusted ID login method.
        name: Trusted IDs
    events:
      trusted_ids:
        verifications:
          invalid:
            email_intro: It has not been possible to grant you the "Trusted IDs" authorization.
            email_outro: Please, contact the support at your platform to check what
              has gone wrong.
            email_subject: Authorization error
            notification_title: Invalid authorization with the "Trusted IDs" method
          ok:
            email_intro: You have been granted the "Trusted IDs" authorization.
            email_outro: You can now perform all actions that require the "Trusted
              IDs" authorization.
            email_subject: Authorization successful
            notification_title: Successful authorization with the "Trusted IDs" method
    system:
      organizations:
        omniauth_settings:
          valid:
            client_id: Client ID
            client_secret: Client secret
            icon_path: Icon (always in the form of media/images/some-image.png)
            scope: Scope
            site: Site url
    verifications:
      trusted_ids:
        errors:
          invalid_method: Could not verify you. OAuth2 verification method is not
            supported.
          invalid_status: Could not verify you. OAuth2 verification was not ok.
          no_identity: Could not verify you. OAuth2 verification did not return
            an identify.
